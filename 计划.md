# 基于多智能体的大学生个性化学习辅助系统 - 开发计划

## 项目概述

本项目是一个 AI 学习辅助系统，采用多智能体协同架构，为大学生提供个性化学习服务。

**技术栈：**
- 前端：Vue3
- 后端：SpringBoot (Java)
- AI层：Python (自研多智能体框架)
- 通讯：HTTP/WebSocket + RPC

---

## 第一阶段：项目基础架构搭建

### 1.1 前端项目初始化 (Vue3)

**目标：** 搭建前端开发环境，建立项目骨架

**步骤：**
1. 安装 Node.js (>=18) 和 pnpm/npm
2. 创建 Vue3 项目：`pnpm create vue@latest frontend`
   - 选择 TypeScript、Vue Router、Pinia
3. 安装依赖：
   - `pnpm add element-plus @element-plus/icons-vue` (UI组件库)
   - `pnpm add axios` (HTTP客户端)
   - `pnpm add markdown-it highlight.js` (Markdown渲染)
   - `pnpm add @vueuse/core` (工具函数)
4. 配置 Vite 代理（开发环境）
5. 配置 ESLint + Prettier
6. 建立目录结构：
   ```
   src/
   ├── api/          # API接口
   ├── components/   # 公共组件
   ├── views/        # 页面
   ├── stores/       # Pinia状态管理
   ├── router/       # 路由
   ├── utils/        # 工具函数
   └── assets/       # 静态资源
   ```

**产出：** 可运行的前端项目架子

---

### 1.2 后端项目初始化 (SpringBoot)

**目标：** 搭建后端开发环境，建立基础架构

**步骤：**
1. 安装 JDK 17+ 和 Maven
2. 创建 SpringBoot 项目 (pom.xml)
3. 引入核心依赖：
   - Spring Web
   - Spring Data JPA / MyBatis-Plus
   - MySQL Driver
   - Redis (缓存)
   - Spring Security (认证授权)
   - JWT
   - WebSocket (实时通信)
   - Lombok
   - Knife4j (API文档)
4. 建立目录结构：
   ```
   src/main/java/com/study/
   ├── config/       # 配置类
   ├── controller/   # 控制器
   ├── service/      # 业务层
   ├── mapper/       # 数据访问层
   ├── entity/       # 实体类
   ├── dto/          # 数据传输对象
   ├── common/       # 通用类
   └── StudyApplication.java
   ```
5. 配置 application.yml (数据库、Redis、日志)
6. 建立统一响应结构 (Result)

**产出：** 可运行的后端项目架子

---

### 1.3 AI层项目初始化 (Python)

**目标：** 搭建AI智能体开发环境

**步骤：**
1. 安装 Python 3.10+ 和 conda/venv
2. 创建项目结构：
   ```
   ai-layer/
   ├── agents/           # 智能体实现
   │   ├── base/         # 基类
   │   ├── teacher/      # 班主任智能体
   │   ├── planner/      # 规划师智能体
   │   ├── doc_generator/# 文档生成器
   │   ├── quiz_generator/# 题库生成器
   │   └── media_generator/# 多媒体生成器
   ├── memory/           # 记忆引擎
   │   ├── vector_store/ # 向量存储
   │   └── structured/   # 结构化存储
   ├── models/           # 模型调用封装
   ├── services/         # AI服务层
   ├── api/              # API接口 (FastAPI)
   └── config.yaml       # 配置文件
   ```
3. 安装核心依赖：
   - `pip install fastapi uvicorn` (Web框架)
   - `pip install openai anthropic` (大模型)
   - `pip install chromadb` (向量数据库)
   - `pip install pydantic` (数据验证)
   - `pip install websocket-client` (WebSocket)
   - `pip install aiohttp` (异步HTTP)
4. 编写模型调用基类（支持OpenAI/Claude等）

**产出：** 可运行的AI层项目架子

---

### 1.4 数据库设计

**目标：** 设计核心数据表结构

**步骤：**
1. 设计用户表 (user)
   - id, username, password, email, create_time, update_time
2. 设计学生画像表 (student_profile)
   - id, user_id, major, grade, knowledge_level, cognitive_style, weak_points, goals, interests
3. 设计技能树表 (skill_tree)
   - id, parent_id, name, description, difficulty_level, status
4. 设计资源表 (learning_resource)
   - id, skill_node_id, type, content, created_by, create_time
5. 设计学习记录表 (learning_record)
   - id, user_id, skill_node_id, resource_id, status, start_time, end_time
6. 设计测验记录表 (quiz_record)
   - id, user_id, skill_node_id, score, answers, create_time

**产出：** SQL建表脚本

---

## 第二阶段：核心功能开发

### 2.1 用户认证模块

**目标：** 实现用户注册、登录、Token认证

**步骤：**
1. 后端：实现UserController (注册/登录/登出)
2. 后端：集成JWT，编写JWT工具类
3. 后端：实现Spring Security配置
4. 前端：实现登录/注册页面
5. 前端：封装Axios请求拦截器（携带Token）
6. 前端：实现Pinia用户状态管理
7. 前端：实现路由守卫

**接口设计：**
- POST /api/user/register
- POST /api/user/login
- POST /api/user/logout
- GET /api/user/info

---

### 2.2 对话式画像构建

**目标：** 通过AI对话自动构建学生学习画像

**步骤：**
1. AI层：实现班主任智能体 (TeacherAgent)
   - 编写画像引导话术模板
   - 实现意图识别
   - 实现字段提取与结构化
2. AI层：实现画像分析智能体 (ProfileAnalyzer)
   - 提取专业、年级、知识基础、薄弱点等
3. 后端：实现画像相关API
   - 保存/更新画像
   - 获取画像摘要
4. 后端：实现与AI层的RPC通信
5. 前端：实现对话式画像构建UI
   - 聊天界面
   - 画像卡片展示
6. 前端：实现WebSocket实时对话

**接口设计：**
- POST /api/profile/init (开始画像构建)
- POST /api/profile/update (更新画像)
- GET /api/profile/summary (获取画像摘要)
- WebSocket /ws/chat (实时对话)

---

### 2.3 技能树管理

**目标：** 实现学习技能树的生成与展示

**步骤：**
1. AI层：实现学习路径规划智能体 (PlannerAgent)
   - 根据画像生成技能树
   - 计算节点难度和依赖关系
2. AI层：实现技能树生成算法
3. 后端：实现技能树相关API
   - 获取技能树
   - 更新节点状态（开始学习/已掌握/测验）
4. 前端：实现技能树可视化组件
   - 使用D3.js或ECharts展示思维导图
   - 实现节点点击交互
   - 实现右键菜单（三种操作）

**接口设计：**
- GET /api/skill-tree (获取技能树)
- PUT /api/skill-node/{id}/status (更新节点状态)
- GET /api/skill-node/{id}/resources (获取节点资源)

---

### 2.4 多智能体资源生成

**目标：** 实现个性化学习资源的自动生成

**步骤：**
1. AI层：实现调度中心 (Scheduler)
   - 接收资源生成任务
   - 协调多个智能体工作
2. AI层：实现文档生成器 (DocGenerator)
   - 生成Markdown格式讲解文档
   - 插入代码示例、避坑指南
3. AI层：实现题库生成器 (QuizGenerator)
   - 生成难度递升的练习题
   - 支持基础题、进阶题、高阶题
4. AI层：实现多媒体生成器 (MediaGenerator)
   - 生成PPT关键帧
   - 生成数字人讲解脚本
5. 后端：实现资源生成任务API
   - 提交生成任务
   - 查询生成进度
6. 后端：实现资源存储与访问
7. 前端：实现资源卡片组件
   - 卡片墙展示
   - 生成进度显示
   - 流式内容展示

**接口设计：**
- POST /api/resource/generate (提交生成任务)
- GET /api/resource/task/{taskId}/status (查询任务状态)
- GET /api/resource/{nodeId} (获取节点资源列表)

---

### 2.5 智能辅导答疑

**目标：** 实现AI智能辅导与多模态答疑

**步骤：**
1. AI层：实现学霸智能体 (TutorAgent)
   - 解答学习问题
   - 提供代码解释
   - 生成举一反三题目
2. AI层：实现薄弱点检测
   - 分析用户提问记录
   - 识别反复出错知识点
3. AI层：实现自动知识节点插入
   - 针对薄弱点生成补充节点
4. 后端：实现答疑相关API
   - 提交问题
   - 获取解答
5. 前端：实现答疑界面
   - 视频内提问按钮
   - 对话式答疑窗口
   - 多模态内容展示

**接口设计：**
- POST /api/qa/ask (提交问题)
- GET /api/qa/history/{skillNodeId} (获取答疑历史)
- POST /api/qa/follow-up (追问)

---

### 2.6 学习效果评估

**目标：** 实现学习效果的跟踪与评估

**步骤：**
1. 后端：实现学习记录服务
   - 记录学习行为
   - 记录测验成绩
   - 计算知识掌握度
2. AI层：实现评估智能体 (EvaluatorAgent)
   - 分析学习投入度
   - 生成评估报告
   - 提供学习建议
3. 后端：实现评估相关API
   - 获取学习报告
   - 获取能力雷达图
4. 前端：实现数据可视化
   - 学习进度展示
   - 能力雷达图
   - 历史记录时间线

**接口设计：**
- POST /api/learning/record (记录学习行为)
- POST /api/quiz/submit (提交测验答案)
- GET /api/evaluation/report (获取评估报告)

---

## 第三阶段：系统集成与优化

### 3.1 前后端联调

**目标：** 确保所有API正常工作

**步骤：**
1. 编写接口文档（使用 Knife4j/Swagger）
2. 前端逐模块对接后端API
3. 处理跨域问题
4. 实现错误处理与提示
5. 优化加载状态展示

---

### 3.2 AI层与后端集成

**目标：** 实现业务层与AI层的无缝对接

**步骤：**
1. 确定通讯协议 (REST/WebSocket/RPC)
2. 实现请求超时与重试机制
3. 实现流式输出支持 (SSE)
4. 配置AI服务地址与认证
5. 实现服务健康检查

---

### 3.3 记忆引擎完善

**目标：** 实现跨会话的上下文保持

**步骤：**
1. 配置向量数据库 (ChromaDB/Milvus)
2. 实现对话历史向量化存储
3. 实现相似知识检索
4. 实现记忆清理与压缩策略

---

### 3.4 性能优化

**目标：** 确保系统响应性能达标

**步骤：**
1. 后端：配置Redis缓存
2. 后端：实现请求限流
3. 前端：实现资源懒加载
4. AI层：实现异步任务队列
5. 数据库：添加必要索引

---

### 3.5 安全加固

**目标：** 确保系统安全可靠

**步骤：**
1. 实现敏感词过滤
2. 实现生成内容校验
3. 配置CORS策略
4. 实现SQL注入防护
5. 实现XSS防护
6. 配置请求签名验证

---

## 第四阶段：部署与上线

### 4.1 环境准备

**步骤：**
1. 准备2-3台服务器
   - 应用服务器 (SpringBoot)
   - AI服务器 (Python)
   - 数据库/缓存服务器
2. 安装 Docker 和 Docker Compose
3. 配置 Nginx 反向代理

### 4.2 项目打包

**步骤：**
1. 前端：`pnpm build` 构建生产版本
2. 后端：`mvn package` 构建JAR包
3. AI层：编写Dockerfile

### 4.3 编写部署文档

**步骤：**
1. 编写 docker-compose.yml
2. 编写 Nginx 配置文件
3. 编写部署脚本
4. 编写运维手册

---

## 开发优先级建议

| 优先级 | 模块 | 理由 |
|--------|------|------|
| P0 | 项目架子 + 用户认证 | 其他模块依赖用户系统 |
| P0 | 对话式画像构建 | 核心差异化功能 |
| P1 | 技能树管理 | 主要交互界面 |
| P1 | 多智能体资源生成 | 核心价值输出 |
| P2 | 智能辅导答疑 | 提升用户体验 |
| P2 | 学习效果评估 | 增强粘性 |
| P3 | 系统优化与部署 | 正式上线前完成 |

---

## 技术注意事项

1. **AI视频生成**：根据实际情况，AI视频生成成本高、效果有限，建议用PPT+数字人讲解代替
2. **数字人方案**：可考虑使用第三方数字人API，而非自建
3. **向量数据库**：ChromaDB适合开发测试，生产环境可考虑Milvus
4. **模型选择**：建议使用Claude API或OpenAI API，成本和效果平衡
5. **前后端通讯**：重要场景使用WebSocket保证实时性

---

*最后更新：2026-05-10*
