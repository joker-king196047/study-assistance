# 基于多智能体的大学生个性化学习辅助系统 - 开发计划

## 项目概述

本项目是一个 AI 学习辅助系统，采用多智能体协同架构，为大学生提供个性化学习服务。

**技术栈：**
- 前端：Vue3 + TypeScript
- 后端：SpringBoot (Java) + MySQL + Redis
- 通讯：HTTP REST API + WebSocket

> AI层将在前端后端完成后单独开发

---

## 当前阶段：第一步 - 搭建前端后端基础架子

---

### Step 1.1：创建前端项目 (Vue3)

**操作：**
1. 确保已安装 Node.js (>=18) 和 pnpm
2. 在项目根目录执行：`pnpm create vue@latest frontend`
   - 选择：TypeScript、Vue Router、Pinia
3. 进入 frontend 目录：`cd frontend`
4. 安装依赖：
   ```bash
   pnpm add element-plus @element-plus/icons-vue axios markdown-it highlight.js @vueuse/core
   ```
5. 安装开发依赖：
   ```bash
   pnpm add -D eslint prettier @types/markdown-it
   ```

**验证：** `pnpm dev` 启动开发服务器，确保项目可运行

**预期产出：** frontend/ 目录下有完整的 Vue3 项目

---

### Step 1.2：创建后端项目 (SpringBoot)

**操作：**
1. 确保已安装 JDK 17+ 和 Maven
2. 在项目根目录创建 backend 目录结构：
   ```
   backend/
   ├── pom.xml
   ├── src/main/java/com/study/
   │   ├── StudyApplication.java
   │   ├── config/
   │   ├── controller/
   │   ├── service/
   │   ├── mapper/
   │   ├── entity/
   │   ├── dto/
   │   └── common/
   └── src/main/resources/
       └── application.yml
   ```
3. 创建 pom.xml，引入依赖：
   - Spring Web
   - Spring Data JPA
   - MySQL Driver
   - Spring Security
   - JWT (jjwt)
   - Redis
   - Lombok
   - Knife4j
   - WebSocket

**验证：** `mvn spring-boot:run` 启动后端，确保项目可运行

**预期产出：** backend/ 目录下有完整的 SpringBoot 项目

---

### Step 1.3：初始化 Git 子模块（可选）

**操作：**
1. 前端作为独立子模块：
   ```bash
   git submodule add <前端仓库地址> frontend
   ```
2. 后端作为独立子模块：
   ```bash
   git submodule add <后端仓库地址> backend
   ```

> 如果前端后端分开管理，可以建立独立仓库

---

### Step 1.4：验证项目结构

**验证清单：**
- [ ] frontend 可以通过 `pnpm dev` 启动
- [ ] backend 可以通过 `mvn spring-boot:run` 启动
- [ ] 前端可以请求后端接口（配置代理）
- [ ] Git 状态干净

---

## 下一步：第二步 - 用户认证模块

完成基础架子后，进入用户认证模块开发：

### 前端任务：
- 创建登录页面 /views/login.vue
- 创建注册页面 /views/register.vue
- 创建首页 /views/home.vue
- 封装 Axios 拦截器
- 创建 Pinia 用户状态管理 /stores/user.ts
- 配置路由守卫

### 后端任务：
- 创建 User 实体类
- 创建 UserMapper
- 创建 UserService + UserServiceImpl
- 创建 UserController (注册/登录/获取用户信息)
- 集成 JWT，实现 JWT 工具类
- 配置 Spring Security
- 实现登录接口：POST /api/auth/login
- 实现注册接口：POST /api/auth/register
- 实现获取用户信息：GET /api/auth/me

### 数据库任务：
- 创建 user 表

---

## 第三步：学生画像模块

### 前端任务：
- 创建对话式画像构建页面
- 实现聊天组件 /components/ChatBox.vue
- 实现画像卡片展示 /components/ProfileCard.vue
- 实现 WebSocket 连接

### 后端任务：
- 创建 StudentProfile 实体类
- 创建画像相关接口：
  - POST /api/profile/init
  - PUT /api/profile/update
  - GET /api/profile/summary
- 创建 WebSocket 端点 /ws/chat

### 数据库任务：
- 创建 student_profile 表

---

## 第四步：技能树模块

### 前端任务：
- 创建技能树页面 /views/skill-tree.vue
- 实现技能树可视化组件（使用 ECharts 或 D3.js）
- 实现节点右键菜单（开始学习/直接测验/标记已掌握）

### 后端任务：
- 创建 SkillTreeNode 实体类
- 创建 LearningResource 实体类
- 创建技能树接口：
  - GET /api/skill-tree
  - PUT /api/skill-node/{id}/status
  - GET /api/skill-node/{id}/resources

### 数据库任务：
- 创建 skill_tree 表
- 创建 learning_resource 表

---

## 第五步：资源生成与学习模块

### 前端任务：
- 创建资源卡片组件 /components/ResourceCard.vue
- 实现 Markdown 渲染组件
- 创建学习页面 /views/learning.vue
- 创建文档查看器
- 创建测验页面 /views/quiz.vue

### 后端任务：
- 创建 Quiz 实体类
- 创建 LearningRecord 实体类
- 创建资源相关接口：
  - GET /api/resource/{nodeId}
  - POST /api/resource/generate
  - GET /api/resource/task/{taskId}/status
- 创建学习记录接口：
  - POST /api/learning/record
  - POST /api/quiz/submit

### 数据库任务：
- 创建 quiz 表
- 创建 learning_record 表

---

## 第六步：智能答疑模块

### 前端任务：
- 创建答疑对话框组件 /components/QADialog.vue
- 在学习页面添加提问按钮
- 实现追问功能

### 后端任务：
- 创建 QA 相关接口：
  - POST /api/qa/ask
  - GET /api/qa/history/{skillNodeId}
  - POST /api/qa/follow-up

### 数据库任务：
- 创建 qa_record 表

---

## 第七步：学习效果评估

### 前端任务：
- 创建评估报告页面 /views/evaluation.vue
- 实现数据可视化（ECharts 雷达图、进度条）
- 创建历史记录时间线

### 后端任务：
- 创建评估相关接口：
  - GET /api/evaluation/report
  - GET /api/evaluation/progress
- 实现评估计算逻辑

---

## 第八步：系统集成与部署

### 前后端联调：
1. 配置 Nginx 反向代理
2. 处理跨域问题
3. 实现统一错误处理
4. 添加加载状态和错误提示

### 性能优化：
1. 前端：路由懒加载、资源压缩
2. 后端：Redis 缓存、数据库索引
3. 实现请求限流

### 安全加固：
1. 配置 CORS
2. 实现敏感词过滤
3. 实现 SQL 注入防护
4. 实现 XSS 防护

### 部署：
1. 前端：构建生产版本 `pnpm build`
2. 后端：构建 JAR 包 `mvn package`
3. 编写 docker-compose.yml
4. 编写部署文档

---

## 开发顺序总结

| 步骤 | 内容 | 前端 | 后端 | 数据库 |
|------|------|------|------|--------|
| 1 | 基础架子 | Vue3项目 | SpringBoot项目 | - |
| 2 | 用户认证 | 登录注册页面 | JWT认证 | user表 |
| 3 | 画像模块 | 对话聊天 | WebSocket | profile表 |
| 4 | 技能树 | 可视化组件 | CRUD接口 | skill_tree表 |
| 5 | 资源学习 | 资源卡片 | 资源管理 | resource表 |
| 6 | 智能答疑 | 答疑对话框 | Q&A接口 | qa表 |
| 7 | 效果评估 | 数据可视化 | 评估计算 | - |
| 8 | 部署上线 | 构建部署 | 构建部署 | - |

---

## 备注

1. **AI层暂不开发**：后端先预留AI调用接口，实际AI功能后续集成
2. **数据库**：开发阶段使用MySQL，可选SQLite简化测试
3. **WebSocket**：使用Spring WebSocket实现实时通信
4. **前端UI**：使用Element Plus组件库，保证界面美观统一

---

*最后更新：2026-05-10*
